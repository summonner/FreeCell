using UnityEngine;
using System.Collections.Generic;

namespace Summoner.FreeCell {
	public enum SoundType {
		MoveCard,
		ResetCards,
		CannotMove,
		NoMoreMove,
		Congraturation,

		MenuButton,
		MenuOpen,
		Stamp,
	}
}